


const config ={
    postgre: {
        host: process.env.POSTGRE_HOST ,
        database: process.env.POSTGRE_DATBASE,
        user: process.env.POSTGRE_USER,
        password: process.env.POSTGRE_PASSWORD
    }
}

export default config;